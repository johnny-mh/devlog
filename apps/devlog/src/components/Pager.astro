---
import SVG from '@jasikpark/astro-svg-loader'

export interface Props {
  prev?: string
  next?: string
}

const { prev, next } = Astro.props
---

<ul class="pager">
  <li class="newer">
    {
      prev ? (
        <a href={prev}>
          <SVG src={import('../images/left.svg?raw')} width={21} height={21} />
          Newer
        </a>
      ) : (
        <span>
          <SVG src={import('../images/left.svg?raw')} width={21} height={21} />
          Newer
        </span>
      )
    }
  </li>
  <li class="older">
    {
      next ? (
        <a href={next}>
          Older
          <SVG src={import('../images/right.svg?raw')} width={21} height={21} />
        </a>
      ) : (
        <span>
          Older
          <SVG src={import('../images/right.svg?raw')} width={21} height={21} />
        </span>
      )
    }
  </li>
</ul>

<style lang="scss">
  @use '../styles/mixins' as *;

  .pager {
    height: 56px;
    margin-bottom: 30px;
    text-align: center;

    gap: 20px;
    display: flex;
    justify-content: center;

    li {
      height: 56px;
      line-height: 56px;
      white-space: nowrap;

      svg {
        width: 21px;
        height: 21px;
        margin-top: -3px;
        vertical-align: middle;
      }

      a {
        @include link;
      }

      span {
        cursor: not-allowed;
        color: #888;

        svg {
          fill: #888;
        }
      }
    }
  }
</style>
