{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["NODE_ENV"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "public/**"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "serve": {
      "dependsOn": ["^build"],
      "cache": false
    },
    "clean": {},
    "typecheck": {}
  }
}
